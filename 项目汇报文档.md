# AeroMind —— 自然语言驱动的无人机智能控制系统

> 项目汇报文档 | 2026年5月25日

---

## 一、项目概述

AeroMind 是一个**基于大语言模型（LLM）的无人机智能控制台**。用户使用中文或英文描述飞行任务（如"去前方30米处搜索车辆"），系统通过 Claude API 解析意图、拆解步骤，自动调用底层飞控工具执行任务，并具备实时 3D 建图与自主避障能力。

**核心价值**：让非专业操作员用自然语言控制无人机完成复杂飞行任务。

---

## 二、系统架构

```
┌─────────────────────────────────────────────────┐
│                   用户界面                       │
│   Web 前端 (HTML/CSS/JS)  │  自然语言输入        │
└─────────────────┬───────────────────────────────┘
                  │ HTTP / WebSocket
┌─────────────────▼───────────────────────────────┐
│              Tornado HTTP Server                 │
│          (server.py — 主服务入口)                │
└─────────────────┬───────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────┐
│             Agent 调度层 (core/)                  │
│                                                  │
│  ┌──────────┐  ┌──────────┐  ┌───────────────┐  │
│  │LLM Client│─▶│Task      │─▶│Agent Tools    │  │
│  │(Claude)  │  │Planner   │  │Runtime (14工具)│  │
│  └──────────┘  └──────────┘  └───────┬───────┘  │
│                                       │          │
│  ┌───────────────────────────────────▼────────┐  │
│  │  3D 感知与规划                              │  │
│  │  ┌──────────────┐  ┌───────────────────┐    │  │
│  │  │OctoMap (3D)  │  │3D A* Path Planner │    │  │
│  │  │概率占据地图   │  │26-邻域路径搜索     │    │  │
│  │  └──────────────┘  └───────────────────┘    │  │
│  └─────────────────────────────────────────────┘  │
│                                       │          │
│  ┌────────────────────────────────────▼───────┐  │
│  │          AirSim Adapter                    │  │
│  │  ┌──────────┐ ┌────────┐ ┌─────────────┐  │  │
│  │  │Telemetry │ │LiDAR   │ │Velocity Ctrl│  │  │
│  │  │Pose/IMU  │ │3D Pts  │ │闭环速度控制  │  │  │
│  │  └──────────┘ └────────┘ └─────────────┘  │  │
│  └────────────────────────────────────────────┘  │
└─────────────────┬───────────────────────────────┘
                  │ RPC
┌─────────────────▼───────────────────────────────┐
│          AirSim / Unreal Engine                  │
│        (高保真物理仿真环境)                       │
└─────────────────────────────────────────────────┘
```

**技术栈**：Python 3.10 | Tornado | NumPy | PyTorch | Claude API | AirSim RPC

**代码规模**：核心模块 32 个 `.py` 文件，测试 8 个文件，总计约 12,000+ 行

---

## 三、核心技术模块

### 3.1 Agent 智能体（`core/agent_loop.py` + `core/llm_client.py`）

- 集成 Claude API，实现 **ReAct（推理-行动）循环**
- LLM 根据当前状态（位置、传感器、任务进度）决定下一步调用哪个工具
- 支持**安全门禁**（`core/safety_gate.py`）：越界、碰撞、危险指令自动拦截
- 支持**工具预览**（任务执行前的风险评估）

### 3.2 任务执行（`core/agent_tools.py` — 14个飞行工具）

| 工具 | 功能 |
|------|------|
| `takeoff` / `land` | 起降控制 |
| `goto_local` | 飞向指定坐标（P控制器） |
| `autonomous_nav` | **闭环自主导航**：实时LiDAR建图 + 3D A*重规划 + 速度控制 |
| `waypoint_route` | 航点序列飞行 |
| `search_area` | 矩形区域覆盖搜索（割草机路径 + 避障） |
| `collect_images` | YOLO数据集自动采集 |
| `formation_flight` | 多机编队飞行 |
| `detect_objects` | YOLO目标检测 |
| `recover_from_collision` | 碰撞恢复（智能方向寻找 + 撤退） |
| `return_home` / `hover` / `stop` | 安全操作 |

### 3.3 3D 感知与地图（`core/octomap_adapter.py`）

- 基于 **OctoMap** 算法的纯 Python 实现
- 使用**对数几率（log-odds）**进行贝叶斯概率更新
- 支持**光线追踪传感器模型**：射线上的体素标记为"自由"，终点标记为"占据"
- **时间衰减**：陈旧观测自动回退到未知状态（0.5概率）
- **概率夹持**：防止单一体素过度自信
- 地图尺寸：160m × 160m × 40m，分辨率 0.5m
- 向量化网格提取：100万单元格仅需 6.5ms

### 3.4 3D 路径规划（`core/path_planner_3d.py`）

- **26-邻域 A\* 搜索**在三维占据网格上
- 代价函数结合**占用概率**：未知区域有可配置的惩罚因子
- 针对实时性能优化：
  - NumPy 数组替代 dict/set（消除哈希开销）
  - 预计算26邻域偏移量与距离
  - 自适应搜索分辨率（1.0m规划，0.5m建图）
- 典型性能：50m距离、230ms 搜索时间

### 3.5 闭环速度控制（`core/airsim_adapter.py`）

- EMA 平滑的速度指令（α=0.65）避免突跳
- 垂直速度硬限幅（±1.5 m/s）
- 距离安全气泡：前方 2.6m / 侧方 1.6m 紧急停车
- 碰撞恢复：智能方向寻找 + 爬升 + 撤退 + 最多2次重试

---

## 四、近期重点优化（2026年5月）

### 4.1 从 2.5D 到真 3D 建图与规划

| 维度 | 旧方案 (TemporalVoxelGrid) | 新方案 (OctoMap) |
|------|---------------------------|-------------------|
| 数据结构 | hit/free 计数器网格 | 对数几率概率网格 |
| 传感器模型 | 简单的命中/未命中计数 | 贝叶斯光线追踪 |
| 时间处理 | 手动计数衰减 | 自动对数几率衰减 |
| 路径规划 | 2D切片投影 + 2D A* | **真3D 26-邻域 A\*** |
| 垂直信息 | 丢失（投影到2D） | 完整保留 |

### 4.2 性能优化（2026-05-24）

- **网格提取向量化**：三重嵌套 Python 循环 → NumPy 广播，**1000倍加速**
- **A\* 搜索优化**：dict/set → NumPy 数组，内联边界检查，预计算邻域
- **配置集中化**：所有导航参数统一在 `config/agent_config.py` 中管理
- 128项测试全部通过

---

## 五、关键数据

### 飞行性能

| 指标 | 数值 |
|------|------|
| 规划分辨率 | 1.0m |
| 建图分辨率 | 0.5m |
| 地图覆盖范围 | 160m × 160m × 40m |
| 重规划间隔 | 0.5s |
| 单次3D路径规划耗时 | ~230ms (50m距离) |
| 网格提取耗时 | ~6.5ms (100万单元格) |
| 最大速度 | 2.0 m/s |
| 避障方式 | 距离传感器 + 3D规划双层保护 |

### 代码质量

| 指标 | 数值 |
|------|------|
| 核心模块 | 32个 |
| 测试文件 | 8个 |
| 测试用例 | 128个 |
| 测试通过率 | 127/128 (99.2%) |
| LLM可调用工具 | 14个 |

---

## 六、项目文件结构

```
AeroMind Console/
├── server.py                 # Tornado HTTP 主服务
├── config/
│   └── agent_config.py       # 集中式导航与安全配置
├── core/                     # 核心模块 (32个)
│   ├── agent_tools.py        # 14个飞行工具实现 (~2300行)
│   ├── agent_loop.py         # LLM ReAct 循环
│   ├── llm_client.py         # Claude API 客户端
│   ├── octomap_adapter.py    # 3D OctoMap 概率占据地图
│   ├── path_planner_3d.py    # 3D A* 路径规划器
│   ├── airsim_adapter.py     # AirSim RPC 封装
│   ├── task_planner.py       # 任务分解与规划
│   ├── safety_gate.py        # 安全门禁
│   ├── validation.py         # 参数验证
│   ├── vision_detector.py    # YOLO 目标检测
│   ├── video_stream.py       # 相机视频流
│   ├── formation_manager.py  # 编队管理
│   ├── structured_log.py     # 结构化日志
│   ├── replay.py             # 轨迹回放
│   ├── preflight.py          # 预扫描评估
│   ├── temporal_grid.py      # [已弃用] 旧版体素网格
│   ├── obstacle_avoidance.py # [已弃用] 旧版2D避障
│   ├── occupancy_grid.py     # [已弃用] 旧版占据网格
│   └── ...
├── tests/                    # 测试 (8个文件, 128个用例)
├── static/                   # Web 前端 (HTML/JS/CSS)
│   ├── js/map.js             # 3D 可视化
│   └── ...
├── docs/                     # 技术文档
└── logs/                     # 运行日志
```

---

## 七、未来方向

1. **多机协同**：编队飞行目前已支持基本队形，后续可扩展协同搜索与任务分配
2. **强化学习导航**：结合 `AirSim-RL-Generalized-Navigation/` 模块，探索端到端策略
3. **OctoMap 持久化**：支持地图保存与加载，跨任务复用
4. **GPU加速**：A* 搜索可移植到 CUDA 进一步提升大规模场景性能
5. **视觉SLAM融合**：结合机载相机实现视觉-激光融合定位

---

## 八、总结

AeroMind 构建了一套完整的**"自然语言 → LLM推理 → 3D感知 → 路径规划 → 飞行控制"**闭环系统。项目近期完成了从2D到真3D建图与规划的核心升级，通过向量化计算大幅优化了实时性能，具备在复杂仿真环境中稳定执行自主导航任务的能力。
