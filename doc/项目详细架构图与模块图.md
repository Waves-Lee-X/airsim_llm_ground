# 项目详细架构图与模块图

> 基线日期：2026-07-15  
> 阅读对象：刚开始学习 ROS 2、PX4、无人机自主系统和大模型 Agent 的项目成员  
> 图表格式：Mermaid。可使用 VS Code Markdown Preview Mermaid、Typora、Obsidian 或 GitHub 查看。

---

## 1. 一张图理解整个项目

```mermaid
flowchart LR
    USER[操作员] -->|自然语言、按钮、确认| UI[Web 地面站]
    UI <-->|HTTP 8080| WEB[ROS Web Console]
    UI <-->|WebSocket / HTTP 8090| GW[Agent Gateway]

    GW --> ROUTER[确定性路由 / LLM Runtime]
    ROUTER --> TASK[Skill + Workflow + Confirmation]
    TASK -->|ExecuteAction Service| AGENT[ROS Agent]

    AGENT -->|控制服务| CONTROL[Control Node]
    AGENT -->|目标 / 取消| AUTO[Autonomy Node]
    AGENT -->|拍照 / 图像分析| PERCEPTION[Perception Node]

    AIRSIM[AirSim] -->|RGB / Depth / LiDAR RPC| BRIDGE[AirSim Bridge]
    PX4[PX4] <-->|uXRCE-DDS| DDS[MicroXRCE-DDS Agent]
    DDS <-->|ROS 2 DDS Topics| BRIDGE
    DDS <-->|ROS 2 DDS Topics| CONTROL

    BRIDGE -->|Image / PointCloud / Odometry| PERCEPTION
    BRIDGE -->|Depth / PointCloud / Odometry| AUTO
    PERCEPTION -->|Detection / ImageAnalysis| AGENT
    PERCEPTION -->|语义建议| AUTO

    AUTO -->|Trajectory| CONTROL
    CONTROL -->|Offboard Setpoint / VehicleCommand| DDS
    PX4 -->|状态、位置、电池、EKF| DDS

    WEB -->|订阅 ROS 状态和传感器| BRIDGE
    WEB -->|状态、任务和图像| UI
    GW -->|流式文本、工具、进度| UI

    classDef human fill:#e8eef7,stroke:#45627d,color:#172638
    classDef web fill:#e3f0ed,stroke:#327367,color:#173a34
    classDef agent fill:#eee8f6,stroke:#76569a,color:#352444
    classDef ros fill:#e8eff8,stroke:#3e6d9c,color:#18344f
    classDef flight fill:#f5ece4,stroke:#9a6845,color:#4c2e1d
    class USER,UI human
    class WEB,GW web
    class ROUTER,TASK,AGENT agent
    class BRIDGE,PERCEPTION,AUTO,CONTROL,DDS ros
    class AIRSIM,PX4 flight
```

这张图可以分成五个层次：

1. **交互层**：用户、Web 地面站和飞书。
2. **智能层**：模型、Skill、Workflow、记忆和确认中心。
3. **ROS 2 能力层**：Agent、感知、自主规划和控制节点。
4. **通信桥接层**：AirSim API Bridge 与 MicroXRCE-DDS Agent。
5. **物理执行层**：PX4、仿真无人机和未来真实无人机。

---

## 2. 运行部署架构图

下面这张图说明各程序通常运行在哪里。

```mermaid
flowchart TB
    subgraph WIN[Windows 主机]
        BROWSER[浏览器<br/>Web 地面站]
        SIM[Unreal Engine + AirSim<br/>虚拟环境、相机、LiDAR]
    end

    subgraph WSL[WSL2 Ubuntu / ROS 2 Humble]
        STATIC[可选静态前端<br/>python http.server :5173]
        WEBSERVER[web_console_node<br/>HTTP :8080]
        GATEWAY[agent_gateway<br/>HTTP + WebSocket :8090]
        ROSNODES[ROS 2 Nodes<br/>Bridge / Agent / Perception<br/>Autonomy / Control]
        XRCE[MicroXRCEAgent<br/>UDP :8888]
        PX4SITL[PX4 SITL]
        DB[(SQLite<br/>会话、事件、确认、记忆)]
        FILES[(missions/<br/>截图与报告)]
    end

    subgraph CLOUD[可选云端模型服务]
        LLM[DeepSeek / Qwen / Claude]
        VLM[Qwen3-VL-Plus]
    end

    BROWSER -->|HTTP| WEBSERVER
    BROWSER -->|可选 HTTP :5173| STATIC
    BROWSER <-->|WebSocket :8090| GATEWAY
    STATIC -->|API :8080 / :8090| WEBSERVER
    STATIC -->|Agent API| GATEWAY

    GATEWAY <--> DB
    GATEWAY --> FILES
    GATEWAY <-->|HTTPS| LLM
    ROSNODES <-->|HTTPS 图像分析| VLM
    GATEWAY <-->|ROS 2 DDS| ROSNODES
    WEBSERVER <-->|ROS 2 DDS| ROSNODES

    ROSNODES <-->|AirSim RPC<br/>通常 TCP 41451| SIM
    PX4SITL <-->|uXRCE UDP 8888| XRCE
    XRCE <-->|DDS| ROSNODES

    classDef box fill:#edf2f7,stroke:#55718c,color:#183047
    classDef ext fill:#f4eee8,stroke:#8a684b,color:#472f1e
    classDef store fill:#eef3e7,stroke:#657c45,color:#30401f
    class BROWSER,STATIC,WEBSERVER,GATEWAY,ROSNODES,XRCE,PX4SITL box
    class SIM,LLM,VLM ext
    class DB,FILES store
```

### 2.1 两条容易混淆的通信链

```text
PX4 数据和控制：
PX4 Client -> UDP 8888 -> MicroXRCEAgent -> DDS -> ROS 2

AirSim 传感器：
AirSim Server -> RPC API -> airsim_bridge_node -> ROS 2 Topic
```

MicroXRCE-DDS Agent 不负责传输 AirSim 图像。AirSim Bridge 也不代替 PX4 飞控。

---

## 3. 软件分层架构图

```mermaid
flowchart TB
    L1[交互层<br/>Web / 飞书 / RViz / ROS CLI]
    L2[智能体层<br/>LLM Runtime / Claude SDK / Memory / MCP]
    L3[任务治理层<br/>Capability / Skill / Workflow / Confirmation / Mission]
    L4[机器人能力层<br/>Perception / Planning / Autonomy / Control]
    L5[ROS 2 中间件层<br/>Topic / Service / Action / TF / DDS]
    L6[飞控与仿真层<br/>PX4 / AirSim / MicroXRCE-DDS]
    L7[硬件层<br/>相机 / IMU / LiDAR / GPS / 电机]

    L1 --> L2 --> L3 --> L4 --> L5 --> L6 --> L7
    L7 -->|传感器反馈| L6
    L6 -->|ROS 消息| L5
    L5 -->|状态与事件| L4
    L4 -->|任务结果| L3
    L3 -->|解释与报告| L2
    L2 -->|流式响应| L1

    SAFE[贯穿式安全约束<br/>白名单、Schema、人工确认、状态验证、超时、悬停、PX4 Failsafe]
    SAFE -.约束.-> L2
    SAFE -.约束.-> L3
    SAFE -.约束.-> L4
    SAFE -.约束.-> L6

    classDef layer fill:#e9f0f7,stroke:#4d6e8c,color:#1e3549
    classDef safety fill:#f7e8e8,stroke:#a25252,color:#4d2020
    class L1,L2,L3,L4,L5,L6,L7 layer
    class SAFE safety
```

核心原则：越接近底层，实时性越高、行为越确定；越接近上层，语义能力越强，但不能绕过底层安全约束。

---

## 4. ROS 2 包模块图

```mermaid
flowchart LR
    IF[aeromind_interfaces<br/>Msg / Srv / Action]
    BR[aeromind_bridge<br/>AirSim + PX4 数据桥]
    PE[aeromind_perception<br/>截图 / YOLO / VLM]
    PL[aeromind_planning<br/>基础路径规划接口]
    AU[aeromind_autonomy<br/>地图 / 避障 / 轨迹]
    CO[aeromind_control<br/>PX4 / AirSim 控制]
    AG[aeromind_agent<br/>ROS 任务执行器]
    GW[aeromind_agent_gateway<br/>多模型 / MCP / Workflow]
    WB[aeromind_web<br/>ROS Web 网关与界面]
    BU[aeromind_bringup<br/>Launch 编排]
    TE[aeromind_teleop<br/>键盘控制]
    PX[px4_msgs<br/>PX4 DDS 消息]

    IF --> BR
    IF --> PE
    IF --> PL
    IF --> AU
    IF --> CO
    IF --> AG
    IF --> GW
    IF --> WB

    PX --> BR
    PX --> CO
    BR --> PE
    BR --> AU
    BR --> CO
    PE --> AG
    PE --> AU
    PL --> AG
    AU --> CO
    AG --> CO
    AG --> AU
    AG --> PE
    GW --> AG
    GW --> AU
    WB --> GW
    TE --> CO

    BU -.启动.-> BR
    BU -.启动.-> PE
    BU -.启动.-> PL
    BU -.启动.-> AU
    BU -.启动.-> CO
    BU -.启动.-> AG
    BU -.启动.-> GW
    BU -.启动.-> WB

    classDef common fill:#f0f2f4,stroke:#66727d,color:#27313a
    classDef sense fill:#e7f2ee,stroke:#47806c,color:#1d4034
    classDef think fill:#eee9f5,stroke:#715b8c,color:#352945
    classDef act fill:#f5ece5,stroke:#936749,color:#482e1e
    class IF,PX,BU common
    class BR,PE sense
    class PL,AU,AG,GW think
    class CO,TE act
    class WB common
```

### 4.1 各包职责

| ROS 2 包 | 主要职责 | 典型输入 | 典型输出 |
|---|---|---|---|
| `aeromind_interfaces` | 统一消息与服务契约 | 无 | `DroneState`、`Trajectory`、`FollowWaypoints` 等 |
| `aeromind_bridge` | AirSim/PX4 数据转换 | AirSim RPC、`/fmu/out/*` | RGB、Depth、PointCloud、Odometry、TF |
| `aeromind_perception` | 检测、截图和语义分析 | RGB、YOLO detection | Detection、ImageAnalysis、PNG |
| `aeromind_planning` | 路径规划服务入口 | 规划请求 | 基础 Path |
| `aeromind_autonomy` | 局部地图、避障和轨迹 | Depth、PointCloud、Odometry、Goal | Trajectory、AutonomyStatus |
| `aeromind_control` | 飞行命令和轨迹跟踪 | Service、Trajectory、Odometry | PX4 setpoint、DroneState |
| `aeromind_agent` | 将结构化动作映射到 ROS 能力 | ExecuteTask、ExecuteAction | Mission 状态、ROS 调用 |
| `aeromind_agent_gateway` | 模型、会话、MCP、确认和 Workflow | Web/飞书消息 | 流式事件、确认、ROS Action |
| `aeromind_web` | ROS 数据转 HTTP/WebSocket | ROS Topic | 浏览器状态、图像和点云 |
| `aeromind_bringup` | 统一启动和参数传递 | Launch 参数 | 完整节点集合 |
| `aeromind_teleop` | 键盘人工控制 | 键盘 | `/control/cmd_vel` |

---

## 5. ROS 2 通信接口总图

图中实线表示 Topic 数据流，虚线表示 Service 或 Action 请求。

```mermaid
flowchart LR
    BRIDGE[airsim_bridge_node]
    YOLO[yolo_detection_node]
    PER[perception_node]
    AUTO[autonomy_node]
    CTRL[control_node]
    AGENT[agent_node]
    GATE[agent_gateway]
    WEB[web_console_node]

    RGB[/sensor/camera/rgb/front_center]
    DEPTH[/sensor/camera/depth/front_center]
    INFO[/sensor/camera/depth/camera_info]
    CLOUD[/sensor/lidar/points]
    ODOM[/sensor/odometry]
    DET[/perception/detections]
    ANALYSIS[/perception/image_analysis]
    ASTATUS[/autonomy/status]
    TRAJ[/autonomy/trajectory]
    DSTATE[/control/drone_state]
    MISSION[/agent/mission_status]

    BRIDGE --> RGB
    BRIDGE --> DEPTH
    BRIDGE --> INFO
    BRIDGE --> CLOUD
    BRIDGE --> ODOM

    RGB --> YOLO
    RGB --> PER
    YOLO --> DET
    DET --> PER
    PER --> ANALYSIS

    DEPTH --> AUTO
    INFO --> AUTO
    CLOUD --> AUTO
    ODOM --> AUTO
    AUTO --> ASTATUS
    AUTO --> TRAJ

    TRAJ --> CTRL
    ODOM --> CTRL
    CTRL --> DSTATE

    DSTATE --> AGENT
    ODOM --> AGENT
    DET --> AGENT
    ANALYSIS --> AGENT
    ASTATUS --> AGENT
    AGENT --> MISSION

    DSTATE --> GATE
    ODOM --> GATE
    DET --> GATE
    ASTATUS --> GATE
    MISSION --> GATE

    RGB --> WEB
    DEPTH --> WEB
    CLOUD --> WEB
    DSTATE --> WEB
    ASTATUS --> WEB

    GATE -. /agent/execute_action .-> AGENT
    AGENT -. /control/takeoff<br/>/control/land<br/>/control/return_home .-> CTRL
    AGENT -. /perception/capture_image<br/>/perception/analyze_image .-> PER
    GATE -. /autonomy/follow_waypoints Action .-> AUTO
    AGENT -. /autonomy/goal .-> AUTO

    classDef node fill:#e7edf4,stroke:#526f8b,color:#1d3449
    classDef topic fill:#eef3e7,stroke:#6f824f,color:#31401f
    class BRIDGE,YOLO,PER,AUTO,CTRL,AGENT,GATE,WEB node
    class RGB,DEPTH,INFO,CLOUD,ODOM,DET,ANALYSIS,ASTATUS,TRAJ,DSTATE,MISSION topic
```

### 5.1 Topic、Service 和 Action 的选择原则

| 方式 | 适合的数据 | 项目示例 |
|---|---|---|
| Topic | 持续、实时、发布者不等待结果 | 图像、点云、状态、里程计、轨迹心跳 |
| Service | 一次请求、较快返回 | 解锁、拍照、图像分析、起飞请求 |
| Action | 长时间任务、需要反馈和取消 | 连续多航点飞行 |
| TF | 坐标系之间的平移和旋转 | `odom -> base_link -> camera/lidar` |

---

## 6. 传感器数据获取与感知模块图

```mermaid
flowchart TB
    subgraph SOURCE[数据源]
        ASCAM[AirSim RGB Camera]
        ASDEPTH[AirSim Depth Camera]
        ASLIDAR[AirSim LiDAR]
        PX4S[PX4 IMU / GPS / Odometry]
    end

    subgraph CONVERT[桥接与标准化]
        RPC[AirSim RPC Client]
        IMGCVT[ImageResponse -> sensor_msgs/Image]
        PCVT[LidarData -> PointCloud2]
        XRCE[MicroXRCEAgent]
        ODOMCVT[PX4 NED/uORB -> ROS ENU/Odometry]
        TF[TF + CameraInfo]
    end

    subgraph CONSUMER[使用数据的模块]
        Y[YOLO<br/>类别、框、置信度]
        P[Perception<br/>截图、VLM、摘要]
        A[Autonomy<br/>三维障碍地图]
        R[RViz / Web<br/>可视化]
        G[Agent<br/>任务解释与报告]
    end

    ASCAM --> RPC --> IMGCVT
    ASDEPTH --> RPC
    RPC --> IMGCVT
    ASLIDAR --> RPC --> PCVT
    PX4S --> XRCE --> ODOMCVT

    IMGCVT --> TF
    PCVT --> TF
    ODOMCVT --> TF

    IMGCVT --> Y
    IMGCVT --> P
    Y --> P
    Y --> G
    P --> G
    IMGCVT --> R
    PCVT --> R
    TF --> A
    A --> G

    classDef source fill:#f4ece5,stroke:#926b4f,color:#432e20
    classDef process fill:#e8eff6,stroke:#537696,color:#1c3850
    classDef use fill:#e9f2eb,stroke:#4e8060,color:#203e2a
    class ASCAM,ASDEPTH,ASLIDAR,PX4S source
    class RPC,IMGCVT,PCVT,XRCE,ODOMCVT,TF process
    class Y,P,A,R,G use
```

### 6.1 RGB 数据

```text
AirSim simGetImages(Scene)
  -> BGRA/BGR 原始字节
  -> sensor_msgs/Image
  -> /sensor/camera/rgb/front_center
```

### 6.2 深度与三维点

```text
AirSim simGetImages(DepthPerspective)
  -> float32 深度图 32FC1
  + CameraInfo 内参
  + TF 外参和无人机位姿
  -> 世界坐标三维障碍点
```

### 6.3 LiDAR 点云

```text
AirSim getLidarData()
  -> [x1,y1,z1,x2,y2,z2,...]
  -> sensor_msgs/PointCloud2
  -> /sensor/lidar/points
```

---

## 7. TF 坐标树

```mermaid
flowchart TB
    ODOM[odom<br/>局部世界坐标]
    BASE[base_link<br/>无人机机体]
    CAMBODY[camera_front_center_body<br/>相机安装坐标]
    CAMOPT[camera_front_center_optical<br/>Z前 X右 Y下]
    LIDAR[lidar<br/>雷达坐标]

    ODOM -->|动态 TF<br/>来自 Odometry| BASE
    BASE -->|静态安装外参| CAMBODY
    CAMBODY -->|固定轴向旋转| CAMOPT
    BASE -->|静态安装外参| LIDAR

    classDef frame fill:#edf1f6,stroke:#526d87,color:#203447
    class ODOM,BASE,CAMBODY,CAMOPT,LIDAR frame
```

TF 解决的是“同一个三维点在不同坐标系中如何表达”。它不存储图像或点云，只存储坐标变换关系。

---

## 8. Agent、Skill 与 Workflow 模块图

```mermaid
flowchart TB
    INPUT[用户自然语言]
    SESSION[SessionManager<br/>会话与流式事件]
    ROUTE{确定性白名单<br/>能否直接识别}
    DR[Deterministic Router]
    MODEL[Claude SDK / DeepSeek / Qwen]
    MCP[MCP Read Tools<br/>状态、里程计、感知]
    CAP[Capability Registry<br/>基础能力 Schema]
    SKILL[Skill Registry<br/>SKILL.md + skill.yaml]
    WF[Workflow Validator<br/>步骤、依赖、条件、循环]
    CONF[Confirmation Center<br/>持久化确认与权限]
    EXEC[RosStateBridge / ROS Agent]
    VERIFY[物理状态验证<br/>Mission / Telemetry / Action]
    MEMORY[(SQLite<br/>消息、事件、记忆、确认)]
    OUTPUT[流式回答、任务进度和报告]

    INPUT --> SESSION --> ROUTE
    ROUTE -->|是| DR
    ROUTE -->|否| MODEL
    MODEL <--> MCP
    MODEL --> CAP
    MODEL --> SKILL
    DR --> WF
    CAP --> WF
    SKILL --> WF
    WF --> CONF
    CONF -->|用户确认| EXEC
    EXEC --> VERIFY
    VERIFY --> OUTPUT
    SESSION <--> MEMORY
    CONF <--> MEMORY
    OUTPUT --> SESSION

    SAFE[禁止 Shell、任意 PX4 消息和未注册动作]
    SAFE -.约束.-> MODEL
    SAFE -.约束.-> WF
    SAFE -.约束.-> EXEC

    classDef process fill:#ebe8f3,stroke:#705a88,color:#352943
    classDef gate fill:#f6ece4,stroke:#966648,color:#4a2c1d
    classDef data fill:#e8f1ec,stroke:#4f7c62,color:#213d2c
    class INPUT,SESSION,DR,MODEL,MCP,CAP,SKILL,WF,EXEC,VERIFY,OUTPUT process
    class ROUTE,CONF,SAFE gate
    class MEMORY data
```

### 8.1 三层能力关系

```text
基础能力 Capability
  例如：takeoff、land、move、follow_waypoints、capture_image
                  ↓ 组合
固定技能 Skill
  例如：正方形、V 字、人员巡检、安全起飞拍照
                  ↓ 动态编排
Workflow
  例如：检查状态 -> 起飞 -> 巡检 -> 条件检测 -> 拍照 -> 返航
```

`SKILL.md` 用来告诉模型技能的语义和使用方法，`skill.yaml` 定义参数与可执行步骤，真正执行的仍是已经注册的 ROS 基础能力。

---

## 9. 自主避障与连续轨迹模块图

```mermaid
flowchart TB
    INPUT[输入<br/>Odometry + Depth + PointCloud + Goal]
    QUALITY{传感器新鲜且<br/>里程计质量合格}
    TF2[TF 转换到世界坐标]
    MAP[Rolling Voxel Map<br/>射线清障 + 衰减 + 裁剪]
    ESDF[ESDF 风格 Clearance Query]
    WAYPOINT{任务类型}
    SINGLE[单目标局部重规划]
    MULTI[连续多航点<br/>Minimum Snap]
    CHECK[未来时间窗碰撞检查]
    SAFE{轨迹安全}
    LOCAL[Kinodynamic Motion Primitives<br/>直飞 / 左右绕 / 爬升 / 下降]
    SPLICE[保留安全短航段<br/>重拼接剩余 Minimum Snap]
    ACTION[FollowWaypoints Action<br/>反馈 / 取消 / 结果]
    TRAJ[/autonomy/trajectory]
    HOLD[安全悬停<br/>SENSOR_WAIT / BLOCKED]

    INPUT --> QUALITY
    QUALITY -->|否| HOLD
    QUALITY -->|是| TF2 --> MAP --> ESDF
    INPUT --> WAYPOINT
    WAYPOINT -->|单点| SINGLE
    WAYPOINT -->|多航点| MULTI
    ESDF --> SINGLE
    ESDF --> CHECK
    MULTI --> CHECK --> SAFE
    SAFE -->|是| ACTION
    SAFE -->|否| LOCAL
    ESDF --> LOCAL
    LOCAL -->|有安全候选| SPLICE --> CHECK
    LOCAL -->|无安全候选| HOLD
    SINGLE -->|安全轨迹| TRAJ
    ACTION --> TRAJ
    HOLD --> TRAJ

    classDef data fill:#e8f0f7,stroke:#527491,color:#1d374d
    classDef decision fill:#f5ece3,stroke:#976847,color:#4a2d1c
    classDef plan fill:#e9f1eb,stroke:#527d60,color:#233f2c
    class INPUT,TF2,MAP,ESDF,TRAJ data
    class QUALITY,WAYPOINT,SAFE decision
    class SINGLE,MULTI,CHECK,LOCAL,SPLICE,ACTION,HOLD plan
```

### 9.1 多航点局部改道过程

```text
原始连续轨迹
  -> 检查未来 3 秒
  -> 发现安全距离不足
  -> 选择安全运动基元
  -> 生成短距离绕行段
  -> 保留短段末端速度和加速度
  -> 对未完成航点重新生成 Minimum Snap
  -> reset_time 帧立即切换控制器时间轴
  -> 继续同一个 ROS 2 Action
```

---

## 10. Control 与 PX4 控制闭环图

```mermaid
flowchart TB
    REQUEST[控制请求]
    MODE{请求类型}
    NATIVE[PX4 原生模式<br/>Arm / Land / RTL]
    TAKEOFF[Offboard 起飞状态机]
    AUTOTRAJ[Autonomy Trajectory]
    TRACK[50 Hz 轨迹插值与跟踪]
    GUARD[速度限幅 / 加速度平滑<br/>最低高度 / 轨迹超时]
    SETPOINT[TrajectorySetpoint<br/>位置 + 速度 + 加速度 + Yaw]
    HEART[OffboardControlMode 心跳]
    COMMAND[VehicleCommand]
    XRCE[MicroXRCEAgent]
    PX4[PX4 Commander + Position Controller]
    MOTOR[姿态控制与电机]
    FEEDBACK[VehicleStatus / Odometry<br/>Battery / GPS / EKF]

    REQUEST --> MODE
    MODE -->|解锁、降落、返航| NATIVE --> COMMAND
    MODE -->|起飞| TAKEOFF --> SETPOINT
    MODE -->|自主飞行| AUTOTRAJ --> TRACK --> GUARD --> SETPOINT
    TAKEOFF --> HEART
    TRACK --> HEART
    SETPOINT --> XRCE
    HEART --> XRCE
    COMMAND --> XRCE
    XRCE --> PX4 --> MOTOR
    PX4 --> FEEDBACK --> XRCE
    XRCE -->|ROS /fmu/out Topics| REQUEST

    MUTEX[控制互斥<br/>TAKEOFF / LAND / RTL 期间<br/>不允许普通自主轨迹抢控制]
    MUTEX -.约束.-> MODE

    classDef control fill:#e9eff6,stroke:#526f8b,color:#1c3449
    classDef flight fill:#f4ece5,stroke:#93694c,color:#482f20
    classDef safety fill:#f5e8e8,stroke:#9b5656,color:#472121
    class REQUEST,MODE,TAKEOFF,AUTOTRAJ,TRACK,GUARD,SETPOINT,HEART,COMMAND,FEEDBACK control
    class NATIVE,XRCE,PX4,MOTOR flight
    class MUTEX safety
```

PX4 负责真正的高频姿态和电机闭环。ROS 2 Control 层发送目标状态，不直接计算每个电机的转速。

---

## 11. Web 数据链路图

```mermaid
flowchart LR
    ROS[ROS 2 Topics]
    WNODE[web_console_node :8080]
    API[HTTP API<br/>状态、图像、点云、截图]
    GATE[agent_gateway :8090]
    WS[WebSocket<br/>流式文本、工具、确认、进度]
    UI[Browser UI]
    THREE[Three.js Point Cloud]
    VIDEO[RGB / Depth Panels]
    CHAT[Agent Chat]
    STATUS[Flight Status / Mission]

    ROS --> WNODE --> API --> UI
    ROS --> GATE --> WS --> UI
    UI --> THREE
    UI --> VIDEO
    UI --> CHAT
    UI --> STATUS
    CHAT -->|用户消息与确认| WS

    classDef server fill:#e8f0f6,stroke:#52728e,color:#1d3549
    classDef ui fill:#e8f2ed,stroke:#4b7b60,color:#203e2d
    class ROS,WNODE,API,GATE,WS server
    class UI,THREE,VIDEO,CHAT,STATUS ui
```

浏览器不能直接消费普通 ROS 2 消息，因此必须由 Web 节点转换：

- ROS `Image` 转为浏览器可显示的 JPEG/PNG。
- ROS `PointCloud2` 抽样并转为 JSON/二进制点集合。
- ROS 状态消息转为 HTTP/WebSocket JSON。
- Agent token 增量转为聊天流式事件。

---

## 12. 复杂任务执行时序图

示例任务：

```text
检查状态，如果安全就起飞到 10 米，飞一个正方形，
发现人就拍照，最后返航并生成报告。
```

```mermaid
sequenceDiagram
    actor U as 用户
    participant UI as Web UI
    participant GW as Agent Gateway
    participant LLM as LLM
    participant CF as Confirmation Center
    participant RA as ROS Agent
    participant C as Control
    participant A as Autonomy Action
    participant P as Perception
    participant PX as PX4

    U->>UI: 输入复杂任务
    UI->>GW: WebSocket message
    GW->>LLM: 状态上下文 + 能力目录
    LLM-->>GW: 结构化 Workflow
    GW->>GW: Schema、白名单、依赖与风险校验
    GW->>CF: 创建确认请求
    CF-->>UI: 显示待确认任务
    U->>UI: 确认执行
    UI->>CF: confirm
    CF->>RA: ExecuteAction / Workflow

    RA->>C: Takeoff 10m
    C->>PX: Arm + Offboard Setpoint
    PX-->>C: Armed / Odometry / Mode
    C-->>RA: 起飞完成

    GW->>A: FollowWaypoints Goal
    A->>A: Minimum Snap + ESDF 检查
    A->>C: /autonomy/trajectory
    C->>PX: 连续 TrajectorySetpoint
    PX-->>C: 位置与速度反馈
    A-->>GW: Action Feedback

    Note over A,C: 若前视轨迹不安全，局部重规划并重拼接剩余航点

    GW->>P: perception_check / analyze_image
    P-->>GW: person + confidence + evidence
    GW->>P: capture_image
    P-->>GW: PNG 路径

    GW->>RA: ReturnHome
    RA->>C: /control/return_home
    C->>PX: PX4 原生 RTL
    PX-->>C: landed + disarmed
    GW->>GW: 汇总状态、截图和事件
    GW-->>UI: 任务报告
    UI-->>U: 显示结果与下载入口
```

---

## 13. 关键模块内部结构

### 13.1 Agent Gateway

```mermaid
flowchart TB
    API[FastAPI / WebSocket API]
    SM[SessionManager]
    RT[Runtime Adapter<br/>Claude / OpenAI-compatible]
    TOOLS[Drone MCP Tools]
    CR[Capability Registry]
    SR[Skill Registry]
    WV[Workflow Validator]
    CC[Confirmation Center]
    RS[RosStateBridge]
    EV[Event Bus]
    ST[(SQLite Store)]
    MEM[Rolling / Semantic Memory]
    FS[Feishu Adapter]

    API --> SM --> RT
    RT --> TOOLS
    TOOLS --> CR
    TOOLS --> SR
    TOOLS --> WV
    WV --> CC --> RS
    RS --> EV --> API
    SM <--> ST
    CC <--> ST
    MEM <--> ST
    MEM --> RT
    FS <--> SM
```

### 13.2 Autonomy

```mermaid
flowchart TB
    SUB[ROS Subscribers]
    QC[Odometry / Sensor Quality Gate]
    PROJ[Depth Projection + TF]
    VM[LocalEsdfMap]
    KR[KinodynamicReplanner]
    MS[Minimum Snap]
    AS[FollowWaypoints ActionServer]
    LC[Task Lifecycle<br/>Arrival / Block / Recovery]
    PUB[Trajectory / Status / Map Publishers]

    SUB --> QC
    SUB --> PROJ --> VM
    QC --> KR
    VM --> KR
    KR --> MS
    AS --> MS
    AS --> KR
    MS --> LC --> PUB
```

### 13.3 Control

```mermaid
flowchart TB
    SERVICES[Arm / Takeoff / Land / RTL Services]
    TSUB[Trajectory Subscriber]
    ODOM[Odometry + PX4 Status Subscribers]
    FSM[Control Mode State Machine]
    INTERP[Trajectory Interpolation]
    LIMIT[Velocity / Acceleration / Altitude Guards]
    PX4C[PX4 Controller Adapter]
    AIRSIMC[AirSim Controller Adapter]
    STATE[DroneState Publisher]

    SERVICES --> FSM
    TSUB --> FSM
    ODOM --> FSM
    FSM --> INTERP --> LIMIT
    LIMIT --> PX4C
    LIMIT --> AIRSIMC
    ODOM --> STATE
```

---

## 14. 安全边界图

```mermaid
flowchart TB
    CMD[模型提出动作]
    S1{能力是否注册}
    S2{参数是否通过 Schema}
    S3{是否需要人工确认}
    S4{状态和传感器是否可用}
    S5{轨迹是否安全}
    EXEC[ROS / PX4 执行]
    VERIFY{遥测是否证明完成}
    DONE[任务完成]
    REJECT[拒绝或要求修改]
    HOLD[安全悬停 / 中止]

    CMD --> S1
    S1 -->|否| REJECT
    S1 -->|是| S2
    S2 -->|否| REJECT
    S2 -->|是| S3
    S3 -->|待确认| REJECT
    S3 -->|已确认或低风险| S4
    S4 -->|否| HOLD
    S4 -->|是| S5
    S5 -->|否且可绕行| S5
    S5 -->|否且不可绕行| HOLD
    S5 -->|是| EXEC
    EXEC --> VERIFY
    VERIFY -->|成功| DONE
    VERIFY -->|超时或失败| HOLD

    classDef pass fill:#e7f1e9,stroke:#4b7e5c,color:#1f3d29
    classDef gate fill:#f4ece3,stroke:#946746,color:#492d1d
    classDef fail fill:#f5e7e7,stroke:#9c5353,color:#482020
    class CMD,EXEC,DONE pass
    class S1,S2,S3,S4,S5,VERIFY gate
    class REJECT,HOLD fail
```

大模型可以提出任务和策略，但不能绕过图中的任何安全门。

---

## 15. 如何使用这些图学习项目

推荐按以下顺序理解：

1. 先看第 1 节，理解系统为什么分层。
2. 看第 2 节，弄清 Windows、WSL、PX4 和云端模型运行在哪里。
3. 看第 5 节，学习 Topic、Service 和 Action 如何连接节点。
4. 看第 6、7 节，理解传感器数据和 TF。
5. 看第 9、10 节，理解避障、轨迹和飞控的分工。
6. 看第 8、12 节，理解大模型如何生成任务但不直接控制电机。
7. 最后使用 ROS 命令逐条验证图中的连接。

常用验证命令：

```bash
ros2 node list
ros2 topic list
ros2 service list
ros2 action list
ros2 topic info /sensor/camera/rgb/front_center -v
ros2 topic info /autonomy/trajectory -v
ros2 action info /autonomy/follow_waypoints
ros2 run tf2_tools view_frames
```

读图时始终追问四个问题：

```text
1. 数据由哪个真实设备或程序产生？
2. 哪个节点把它转换成 ROS 消息？
3. 通过 Topic、Service 还是 Action 传输？
4. 哪个模块最终使用它做出什么行为？
```

只要能沿着图回答这四个问题，就已经掌握了该模块的基本架构。
